/**
 * User roles for role-based access control
 */
export enum Role {
  CLIENT = 'client',
  SOLANACER = 'solanacer',
  DEGEN = 'degen',
  ADMIN = 'admin',
  SUPER_ADMIN = 'super_admin',
  GUEST = 'guest',
  ANONYMOUS = 'anonymous',
  SYSTEM = 'system',
  API = 'api',
}
